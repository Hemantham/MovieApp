 // Find all classes with a name ending with Model
    class MovieInfo {
        constructor(public type: MovieType, public id: string, public title: string, public year: number, public imageUrl: string, public provider: string) {

        }
    }
 